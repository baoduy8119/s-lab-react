version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sLab
    ports:
      - "8010:3000"
    restart: always
    environment:
      NODE_ENV: production
